import 'package:flutter/material.dart';

class ListPlaceHolder extends StatelessWidget {

  @override
    Widget build(BuildContext context) {
      // TODO: implement build
      return new Text('Place Holder');
    }
}